{
  "builds": [
    {
      "src": "vite.config.js", 
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      },
      "buildCommand": "vite build"  
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
